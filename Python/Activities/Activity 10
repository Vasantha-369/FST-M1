# Given tuple
num_tuple = input("Enter a list element separated by space ")
listRange  = num_tuple.split()



# Print elements that are divisible by 5
print("Elements that are divisible by 5:")
for num in listRange:
    num = int(num)
    if (num % 5 == 0):
        print(num)